# 🌌 별자리 운명의 그물 — GraphRAG 심층 운세 앱

> 태양궁 · 달궁 · 상승궁의 세 에너지 관계망을 Knowledge Graph로 분석하는 업그레이드 별자리 운세 앱

## 🔗 기존 앱 대비 업그레이드 포인트

| 기존 (astrol108-1) | 업그레이드 (GraphRAG) |
|---|---|
| 태양궁 1개만 입력 | 태양궁 + 달궁 + 상승궁 3개 입력 |
| 단순 별자리 운세 | 세 별자리 **관계망** 기반 심층 해석 |
| AI가 왜 그런지 설명 없음 | **추론 경로(엣지)를 시각화**해서 근거 제시 |
| 텍스트 결과만 | 인터랙티브 **Knowledge Graph 시각화** |
| 일반 LLM 프롬프트 | GraphRAG 방식 **구조화 프롬프트** |

## 🧠 GraphRAG 핵심 개념 적용

```
[태양궁 노드] ─삼분(Trine)─ [달궁 노드] ─대립(Opposition)─ [상승궁 노드]
       │                          │                              │
   [화성 노드]              [수성 노드]                    [금성 노드]
   (지배행성)               (지배행성)                    (지배행성)
```

각 별자리를 Knowledge Graph의 **노드**로, 각도·원소·지배행성 관계를 **엣지**로 구성해
단순 검색이 아닌 관계 경로를 따라 운세를 추론합니다.

## 🚀 빠른 시작

### 1. 레포지토리 클론

```bash
git clone https://github.com/your-id/astro-graphrag.git
cd astro-graphrag
```

### 2. Vercel 배포

```bash
# Vercel CLI 설치
npm i -g vercel

# 배포
vercel

# 환경 변수 설정
vercel env add OPENAI_API_KEY
# → OpenAI API 키 입력
```

### 3. 또는 GitHub → Vercel 연결 (추천)

1. GitHub에 push
2. [vercel.com](https://vercel.com) → New Project → 레포 선택
3. Environment Variables에 `OPENAI_API_KEY` 추가
4. Deploy!

## 📁 파일 구조

```
astro-graphrag/
├── index.html          # 메인 앱 (단일 파일)
├── api/
│   └── fortune.js      # Vercel 서버리스 함수 (GraphRAG 프롬프트)
├── vercel.json         # Vercel 설정
└── README.md
```

## 🔑 환경 변수

| 변수명 | 설명 |
|---|---|
| `OPENAI_API_KEY` | OpenAI API 키 (gpt-4o-mini 사용) |

> 💡 Claude API로 전환하려면 `api/fortune.js`의 fetch URL과 헤더를 Anthropic API로 변경하세요.

## 🛠️ Claude API로 전환하는 방법

`api/fortune.js`에서 아래 부분을 교체:

```js
// OpenAI 대신 Claude 사용 시
const response = await fetch('https://api.anthropic.com/v1/messages', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'x-api-key': process.env.ANTHROPIC_API_KEY,
    'anthropic-version': '2023-06-01'
  },
  body: JSON.stringify({
    model: 'claude-haiku-4-5-20251001',
    max_tokens: 1000,
    system: systemPrompt,
    messages: [{ role: 'user', content: userPrompt }]
  })
});
const aiData = await response.json();
const content = aiData.content[0].text;
```

## 📊 GraphRAG 데이터 구조

### 별자리 Knowledge Graph 노드

각 별자리는 다음 속성을 가진 노드입니다:
- `element`: 원소 (불/흙/바람/물)
- `quality`: 성질 (활동/고정/변통)
- `ruler`: 지배행성
- `polarity`: 극성 (양/음)
- `keywords`: 핵심 키워드 배열
- `compatible`: 호환 별자리 목록
- `tension`: 긴장 별자리 목록

### 엣지 (관계)

- **각도(Aspect)**: 합·육분·삼분·직각·대립 등
- **원소 관계**: 상생·충돌·동일 원소
- **지배행성 공유**: 같은 행성 지배 여부

## 🎯 향후 확장 아이디어

- [ ] 생년월일 입력으로 달궁·상승궁 자동 계산
- [ ] 두 사람 비교 궁합 분석
- [ ] 오늘의 행성 위치 실시간 반영
- [ ] Neo4j 연동으로 진짜 GraphRAG 구현
- [ ] 개운 아이템 쇼핑몰 연결

## 📜 라이선스

MIT
